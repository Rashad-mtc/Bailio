import Link from "next/link";
import { PortalShell, PageHeader } from "@/components/portal-shell";

export default async function BienDetailPage({ params }: { params: Promise<{ bienId: string }> }) { await params; return <PortalShell><PageHeader eyebrow="MES BIENS / DÉTAIL" title="Résidence Les Palmiers" description="Cocody Riviera 3, Abidjan" action={<Link className="secondary-button" href="/biens">← Retour aux biens</Link>} /><div className="detail-stats"><div className="mini-stat"><span>Logements</span><strong>12</strong></div><div className="mini-stat"><span>Occupés</span><strong className="positive">11</strong></div><div className="mini-stat"><span>Revenus mensuels</span><strong>2 120 000 FCFA</strong></div></div><section className="panel"><div className="panel-heading"><div><h2>Logements de la résidence</h2><p>Suivez le statut de chaque logement.</p></div><button className="primary-button" type="button">+ Ajouter un logement</button></div><div className="unit-grid">{["A-01", "A-02", "A-03", "A-04", "B-01", "B-02", "B-03", "B-04"].map((unit, index) => <div className="unit-card" key={unit}><div><strong>{unit}</strong><span>2 pièces · Étage {index % 2 + 1}</span></div><span className={`status ${index === 4 ? "pending" : "paid"}`}>{index === 4 ? "Disponible" : "Occupé"}</span><strong>{index === 4 ? "--" : `${150 + index * 5} 000 FCFA`}</strong></div>)}</div></section></PortalShell>; }
