import Link from "next/link";

export function AuthCard({ register = false }: { register?: boolean }) {
  return <main className="auth-page"><Link className="brand auth-brand" href="/"><span className="brand-mark">L</span><span>Loyer<span className="brand-accent">Facile</span></span></Link><section className="auth-card"><div className="auth-card-heading"><span className="auth-symbol">{register ? "✦" : "↗"}</span><p className="eyebrow">BAILIO</p><h1>{register ? "Créez votre compte" : "Bon retour parmi nous"}</h1><p>{register ? "Choisissez votre espace pour commencer." : "Connectez-vous pour accéder à votre espace."}</p></div><form className="form-stack"><label>Numéro de téléphone<input type="tel" placeholder="+225 07 00 00 00 00" required /></label>{register && <label>Nom complet<input type="text" placeholder="Votre nom et prénom" required /></label>}{register ? <fieldset className="role-fieldset"><legend>Je m’inscris en tant que</legend><label className="role-option"><input type="radio" name="role" value="proprietaire" defaultChecked /><span><strong>Propriétaire</strong><small>Je gère mes biens, mes locataires et mes paiements.</small></span></label><label className="role-option"><input type="radio" name="role" value="locataire" /><span><strong>Locataire</strong><small>Je paie mon loyer, mes factures SONEB/SBEE et demande de l’aide.</small></span></label></fieldset> : <label>Mot de passe<input type="password" placeholder="••••••••" required /></label>}<button className="primary-button wide-button" type="submit">{register ? "Créer mon compte" : "Se connecter"} <span>→</span></button></form><div className="auth-separator"><span>ou</span></div><p className="auth-switch">{register ? "Vous avez déjà un compte ? " : "Vous n’avez pas encore de compte ? "}<Link href={register ? "/login" : "/register"}>{register ? "Se connecter" : "Créer un compte"}</Link></p></section><p className="auth-footer">En continuant, vous acceptez nos conditions d’utilisation.</p></main>;
}
